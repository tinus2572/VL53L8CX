#![deny(unsafe_code)]
#![no_main]
#![no_std]

use cortex_m_rt::entry;
// use core::{convert::TryFrom, fmt::Debug, u8};
// use cortex_m_semihosting::hprint;
// use embedded_hal::delay::DelayNs;
use panic_halt as _;

#[entry]
fn main() -> ! {

    loop {

    }
   
}